# Marrow

Monorepo for the Marrow project (formerly PMem / Project Memory MCP): shared MCP memory gateway (`backend/`) and its web UI (`front/`).

History of both halves was preserved via `git subtree` from their previous separate repositories.
